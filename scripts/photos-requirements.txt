# Pinned for scripts/process_guest_images.py via scripts/setup-photos-venv.sh.
# setuptools<81 is load-bearing: face_recognition_models imports
# pkg_resources, which setuptools removed in 81.
Pillow
face_recognition
setuptools<81
# For scripts/normalize_photos.py — iPhones upload HEIC, which browsers can't
# render and process_guest_images.py skips. See scripts/ci-requirements.txt
# for the Linux equivalent used by the Actions sync.
pillow-heif
